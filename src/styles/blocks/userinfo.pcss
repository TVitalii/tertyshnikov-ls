.userinfo {
  display: flex;
  align-items: flex-start;
  justify-content: center;
}

.userinfo__title {
  color: rgba($text-color, .4);
  font-weight: bold;
  text-align: right;
  margin-bottom: 20px;
  margin-right: 60px;
  padding-top: 10px;
}

.userinfo__name {
  font-size: 72px;
  font-weight: 800;
  line-height: 1;
  flex-basis: 400px
}

.userinfo__occ {
  white-space: nowrap;
}

.userinfo__name {
  font-weight: 800;
  font-size: 72px;
  line-height: 1;
}