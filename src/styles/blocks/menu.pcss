.menu__list {
  display: flex;
  justify-content: center;
}

.menu__item {
  margin-right: 60px;
}
.menu__link {
  text-decoration: none;
  white-space: nowrap;
  font-weight: 600;
  &:hover {
      color: #4a00ed;
    }
}