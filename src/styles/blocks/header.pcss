.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header__menu {
  width: 55%;

  @include tablets {
    display: none;
  }
}

.header__buttons {
  @include phones {
    display: none;
  }
}